import RecordButton from "@/components/RecordButton";

export default function Home() {
  return (
    <main className="flex flex-col min-h-screen items-center justify-center bg-slate-100 px-4 py-8">
      <RecordButton />
    </main>
  );
}
